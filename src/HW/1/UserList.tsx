type AddressType = {
  street: any; 
  city: any;
};

type UserType = {  
};

type UserListPropsType = {
  users: UserType;
};

export const UserList = () => {
  return (
    <div id={'hw01-users'}>
      
    </div>
  );
};
